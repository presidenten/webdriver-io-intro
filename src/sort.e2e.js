describe('User list', () => {
  beforeEach(() => {
    browser.url('localhost:8080');
  });

  it('should be able to sort', () => {
    // Write this test
    expect(false).toBe(true); // Remove this line
  });
});

